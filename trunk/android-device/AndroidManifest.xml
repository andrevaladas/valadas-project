<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.chronosystems.view"
      android:versionCode="1"
      android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application android:icon="@drawable/icon_device" android:label="@string/app_name" android:theme="@style/CustomTheme">
        <activity
            android:label="@string/app_name"
            android:name=".TabDashboardActivity"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  Login Activity -->
        <activity
            android:label="Login Account" 
            android:name=".LoginActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>

        <!--  Register Activity -->
        <activity
            android:label="Register New Account" 
            android:name=".RegisterActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>

        <!-- Home Activity -->
        <activity
            android:label="Home" 
            android:name=".HomeActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>

        <!-- Following List Activity -->
        <activity
            android:label="Following" 
            android:name=".FollowingListViewActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>

        <!-- Search List Activity -->
        <activity
            android:label="Search" 
            android:name=".ListViewActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
        <!-- Places Activity -->
        <activity
            android:label="Places" 
            android:name=".PlacesViewActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
        <!-- Maps Location Activity -->
        <activity
            android:label="Map Location" 
            android:name=".MapViewActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
        <activity
            android:label="PlacesHorzScrollListMenu" 
            android:name=".PlacesHorzScrollListMenu"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
        <uses-library android:name="com.google.android.maps" android:required="true"/>
    </application>
    
</manifest>